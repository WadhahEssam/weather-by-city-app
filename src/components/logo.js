import React , { Component } from 'react' ;

const Logo = ( ) => {
    return (
        <div className={'row  justify-content-center'}>
            <img className={'logo'} height={100} src="/images/logo.jpg" />
        </div>
    ) ;
};

export default Logo ;